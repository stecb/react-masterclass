/*
  LEVEL: EASY

  How to build an element based 
  on the type of passed props.

  What if we want a "smart" button, so it
  can render dynamically as a <button> "normally",
  but if we pass href, it will render as an <a>
*/

import React from 'react';

export default function DynamicElement() {
  return (
    <div>DynamicElement</div>
  )
}
